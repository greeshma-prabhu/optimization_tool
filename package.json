{
  "name": "zuidplas-optimization",
  "version": "1.0.0",
  "description": "Zuidplas Logistics Route Optimization System",
  "main": "index.html",
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p public && cp *.html public/ && cp -r js public/ && cp -r css public/ 2>/dev/null || true",
    "start": "node proxy-server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.x"
  }
}
